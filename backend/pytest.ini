[pytest]
# Make the backend modules (app, services, auth, models, dimensions) importable
# from anywhere under tests/ without requiring cwd=backend/.
pythonpath = .
testpaths = tests
# Powertools warns when a handler publishes no metrics; harmless in tests.
filterwarnings =
    ignore:No application metrics to publish.*:UserWarning
